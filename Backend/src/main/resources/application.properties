spring.application.name=Backend
spring.datasource.url=jdbc:mysql://localhost:3306/eventdb?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=tiger
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT secrets
app.jwt.secret=ChangeThisSecretToSomethingStrong
app.jwt.expiration-ms=3600000

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=aq199075@gmail.com
spring.mail.password=gkspozbdiqpsqjoi
spring.mail.protocol=smtp

# SMTP properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=false

# Optional: timeouts
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Debugging
spring.mail.properties.mail.debug=true

# CORS origin for frontend
app.frontend.origin=http://localhost:5173

# Increase max HTTP header size to 32KB
server.tomcat.max-http-header-size=32768

# Optional: increase POST size for file uploads (50 MB)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
